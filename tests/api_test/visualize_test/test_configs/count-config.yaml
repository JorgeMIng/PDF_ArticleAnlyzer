# Word Cloud server-config
data:
  data_dir: "data_tests/PDFs"
  format: ".pdf"
  recursive: True

grobid:
  cache: True
  cache_dir: "data_tests/xmls"
  operation_key: "processFulltextDocument"
  format: ".grobid.tei.xml"
  recursive: True

plot:
  width: 0.8
  cache_dir: "data_tests/img/count_img"
  format: ".png"
  align: "center"
  color: "blue"
  edgecolor: 'black'

#["figures","ref","p"] for more than one stat
count:
  stats: "figure" 